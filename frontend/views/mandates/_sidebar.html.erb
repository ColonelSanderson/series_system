<%= render(:layout => '/shared/sidebar',
           :locals => {
               :record_type => 'mandate',
               :record => @mandate,
               :plusone => :submit
           }) do |sidebar| %>
  <%= sidebar.render_for_view_and_edit(:subrecord_type => 'date', :property => 'date') %>
  <%= sidebar.render_for_view_and_edit(:subrecord_type => 'external_id', :property => 'external_ids') if show_external_ids? && sidebar.show_external_ids_sidebar_entry? %>
<% end %>
